# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# souschef local usage store (dev metering)
server/usage.json
server/leaderboard.json

# Capacitor native projects — regenerate with:
#   npx cap add ios && npx cap add android && npx @capacitor/assets generate --ios --android
# Kept out of git: they bloat the repo Railway deploys from and broke its build snapshot.
ios
android
